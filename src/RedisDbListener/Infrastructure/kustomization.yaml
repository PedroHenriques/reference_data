apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: ${SERVICE}-config
    files:
      - values.yaml=values.yaml

generatorOptions:
  disableNameSuffixHash: true

namespace: ${NAMESPACE}
resources:
  - secret.yaml
  - helmrelease.yaml
