architecture: standalone
auth:
  enabled: true
  existingSecret: ${SERVICE}-secret

master:
  service:
    ports:
      redis: ${DBLISTENER_REDIS_CON_PORT}
  persistence:
    enabled: true
    size: ${DBLISTENER_REDIS_PV_SIZE}
    accessMode: ReadWriteOnce

global:
  storageClass: managed-csi